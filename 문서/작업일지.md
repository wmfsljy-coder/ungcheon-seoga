# 작업 일지

무엇을 왜 그렇게 정했는지 적어 둡니다. **다른 계정에서 이어 작업할 때 이 파일부터 읽으세요.**
새로 고칠 때마다 맨 아래에 한 줄씩 더합니다.

## 무엇인가

웅천고 전교생 독서 프로그램 '웅천 가을 서가' 웹앱(2026 추석 이후 시작, 고3 취약시기용).
학생이 책을 읽고 **라벨(한 줄 소개) · 독후감 · 북퀴즈**를 남기면 낙엽 도장이 쌓이고, 도장 5개마다 별 1개, 별 2개마다 문화상품권을 받습니다.

- 시트: `1VXcAbIQPTyUC1eIDFvVh5ZWLYLyatSeTEUH_a-IeBYg` · 스크립트: `1u4WxxQA3…`(`.clasp.json`)
- 구조: `core.gs`(규칙) · `Code.gs`(시트·메일·트리거) · `index.html`(화면) · `tools/`(빌드·시험·배포)
- 배포: `python tools/deploy.py "설명"` — 클로드가 직접 합니다(2026-09-19 회장님: "전부 니가 자동으로 적용시켜"). 회장님이 하실 일은 권한이 바뀔 때 한 번 승인하는 것뿐.

## 정해 둔 것 (되돌리려면 까닭부터 보기)

| 정한 것 | 까닭 |
| --- | --- |
| 라벨은 프린텍 V3270(100×46.4mm 12칸), 제목이 가장 크게 | 책에 붙여 두고 멀리서도 읽히게 |
| 바로 게시가 기본, 인쇄 전 담당이 훑는 것이 관문 | 승인 대기를 만들면 글이 안 올라옴 |
| 투표는 지난주 글 8편 무작위·득표 비공개·투표자는 해시만 | 인기 투표로 흐르지 않게, 개인정보 안 남게 |
| 읽은 계기를 필수로 | AI로 지어낸 글 억제 |
| 학생 문제 내기는 무제한(같은 문제·욕설만 막음) | 많이 내는 것 자체가 독서 활동 |
| 이번 주 북퀴즈는 서가 책만 | 학교에 없는 책이 문제로 나오면 풀 수가 없음 |
| 알림 창(alert) 절대 금지 | 시트에 묶인 스크립트에서 alert 을 쓰면 누가 확인을 누를 때까지 실행이 멈춤 |
| 학생 눈높이 문구 | 회장님: "학생들이라는 점 잊지 마" — 고등학생에게 유치하지 않게, 부담 주지 않게 |

## 거쳐 온 것

- **1차** 아티팩트 시연본만 있던 것을 소스로 재구성. 시트+Apps Script 로 올림.
- **2차** 참여 장치: 읽고 싶어요 공감, 선생님 라벨, 학급 참여율, 참여자 추첨, 후배 열람. 설치 안내 페이지(`tools/build_paste_page.py`).
- **3차** 독서로(read365) 자동 확인 — 학교 검색 화면이 쓰는 공개 API 로 소장·대출 상태를 매일 확인(로그인 불필요). 이달의 서가(매달 1일 자동 선정), 도서 검색 탭, 개인정보 동의.
- **4차** 꾸밈: 제목 주아체 · 본문 Pretendard · 교표 · 마스코트 **웅이**(웅천=熊川의 곰) SVG와 말풍선.
- **5차** 북퀴즈 자동 출제(학생 문제 품질 10점 기준, 모자라면 청구기호·지은이·분류로 자동 생성 — 지어낸 내용 없음). 웅이가 새 도장을 알아보고 칭찬.
- **6차** '내 서재' 탭(아림 갤러리 '내 작품'처럼): 누적 통계·표지 책장·책별 상세·북퀴즈 기록.
- **7차** 교사 새 소식(마지막확인 뒤 새 글·등록·완주·문제 채택) + 교사 독서 기록 탭(생기부 독서활동상황용 복사·CSV).

## 겪은 함정

- **설정 시트는 setup 이 처음 한 번만 채웠다** → 나중에 늘린 항목이 시트에 안 보였습니다(회장님 지적). 지금은 `maintain()` 이 빠진 항목과 설명 칸을 채웁니다. **새 설정 항목을 만들면 `CONF0` 과 `CONF_DESC` 둘 다에 넣으세요.**
- setup 이 오래 걸리던 까닭 둘: 끝에서 독서로 600여 건을 기다림, `say_` 가 `getUi().alert` 이라 실행이 멈춤. 지금은 setup 은 빠른 일만 하고 독서로는 30초 뒤 `firstRun` 이 합니다.
- 배지 클래스 이름 `busy` 가 로딩 막대와 부딪혔습니다 → `.st.out`.
- 셸 heredoc 에 한글 파이썬 패치를 넣으면 깨질 때가 있습니다 → 패치 스크립트는 파일로 써서 실행하세요.
- `Code.gs` 는 `tools/test_code_gs.js`(가짜 구글 서비스)로 돌려 볼 수 있습니다.

## 못 하는 것

- 클로드는 회장님 **구글 시트를 읽을 수 없습니다**. 시트 쪽은 회장님이 눈으로 확인해 주셔야 합니다.
- 실제 메일 발송·Apps Script 속도는 이 환경에서 재 본 적 없습니다.
