# 웅천 서가 — 같이 작업하는 규칙

이 파일은 **어느 클로드 계정으로 들어오든 똑같이 일하도록** 적어 둔 것입니다. 작업을 시작하기 전에 이 파일부터 읽습니다.

## 0. 시작할 때 · 끝낼 때

```bash
git pull --rebase          # 시작 전에 반드시(다른 계정에서 고쳐 놓았을 수 있음)
...작업...
git add -A && git commit -m "무엇을 왜 고쳤는지" && git push
```

- **작업이 끝나면 바로 커밋·푸시합니다.** 안 하면 다른 계정에서 옛 코드를 고쳐 부딪칩니다.
- 커밋 메시지는 한국어로 한 줄.
- 두 계정에서 **동시에** 고치지 않습니다. 한쪽이 끝내고 푸시한 뒤 다른 쪽이 pull 합니다.
- 이 저장소는 **작업 폴더 `C:\Users\user\Desktop\클로드\웅천서가`** 가 원본입니다. (`ungcheon-seoga` 폴더는 같은 저장소를 한 번 더 받아 둔 것이니 헷갈리면 이 폴더를 쓰세요.)

## 1. 회장님(사용자)께 말하는 법

- 한국어로, 선생님께 설명하듯 쉬운 말로. 영어 낱말·전문 용어는 되도록 풀어서.
- 코드를 붙여 보여 주지 말고, **무엇이 어떻게 달라지는지**를 말합니다.
- 못 한 것·확인 못 한 것은 그대로 말합니다. 되는 척하지 않습니다.

## 2. 고칠 때 지키는 차례

```bash
python tools/build_demo.py        # 시연본(demo.html) 다시 만들기
node tools/tests/<하나>.js        # 관련 시험
python tools/deploy.py "설명"      # 문법·시험 전부 → clasp push → 같은 주소로 새 버전 → 앱 한 번 열기
```

- `deploy.py` 가 `Code.gs` 의 `CODE_VERSION` 을 자동으로 갱신합니다(직접 고치지 않습니다).
- 시험은 `tools/tests/` 에 시나리오별로 있습니다(계정·도서목록·상품권 안내·메일 로그인·별·역할·달별 퀴즈 등). **새 기능은 시험을 함께 씁니다.**
- 화면을 고쳤으면 `demo.html` 을 브라우저로 열어 눈으로 확인합니다.

## 3. 절대 하지 말 것

- `.clasprc.json`(구글 토큰)에서 토큰을 꺼내려 하지 않습니다. clasp 명령으로만 씁니다.
- 시트 탭 머리줄 이름을 함부로 바꾸지 않습니다. 바꿔야 하면 옛 시트를 따라잡는 손질을 같이 씁니다.
- 학생 개인정보(이름·학번)를 저장소나 아티팩트에 넣지 않습니다. 시연본 자료는 가짜 이름만 씁니다.

## 4. 알아 둘 것

- 시트: `https://docs.google.com/spreadsheets/d/1VXcAbIQPTyUC1eIDFvVh5ZWLYLyatSeTEUH_a-IeBYg/edit`
  **클로드는 이 시트를 읽을 수 없습니다.** 시트 쪽 확인이 필요하면 코드로 만들고 회장님께 눈으로 봐 달라고 부탁합니다.
- 배포 주소·마지막 배포 기록: `tools/last_deploy.txt`(저장소에는 안 올라감).
- `.clasp.json` 은 이 저장소에 들어 있습니다(스크립트 ID). 그래서 `clasp push` 가 이 폴더에서 바로 됩니다.
- 설치·운영 절차는 `설치안내.md`, 얼개는 `README.md`.
- 자매 프로젝트: **아림 갤러리**(미술 작품 전시, 저장소 `wmfsljy-coder/arim-gallery`). 구조가 같아서(core.gs·Code.gs·index.html·tools) 한쪽에서 만든 장치를 옮겨 쓰기 좋습니다.

## 5. 배경이 궁금하면

`문서/작업일지.md` — 무엇을 왜 그렇게 정했는지 적어 둡니다. **이어서 작업하기 전에 읽고, 새 판을 올렸으면 한 줄 더합니다.**
